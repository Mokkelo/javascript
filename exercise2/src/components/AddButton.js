/*import React from 'react';

export default function AddButton(props) {
    function onAddMaitoaClick(element) {
        props.buttonClickedEvent(element);
        console.log("123123")
    }

  return( 
  <div>
      <ul> {
            props.listItems.map((item, index) => {

                console.log("123123")
            //if(item.name == )
            //let classValue = "";
            //if(item.isChecked){
            ///\    classValue = "isChecked";
            //}
            return <li onClick={() => onAddMaitoaClick(item) } key={index} className={item.name ? "Maitoa" : null} > 
            {item.qty} { item.name }
            </li>;
  }
            )
}
  
  
  
  </ul>
  </div>)
  

}
*/
